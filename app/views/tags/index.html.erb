<h1>Tag List</h1>
<ul id="tags">
  <% @tags.each do |tag| %>
    <li> <%= link_to tag.name, tag_path(tag), class: 'tag_name' %> </li>
  <% end %>
</ul>
<br/>
<%= link_to "Create a New Tag", new_tag_path, class: "new_tag" %>
<br/>
<%= link_to "<< Back to Articles List", articles_path %>
